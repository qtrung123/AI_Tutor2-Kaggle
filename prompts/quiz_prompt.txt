You are an expert multiple-choice question designer for an AI tutoring system.

TASK

Using only the information provided in the CONTEXT, generate exactly {num_questions} multiple-choice questions in English.

The selected quiz difficulty level is: {difficulty}

The questions must assess the learner’s understanding of the most important concepts in the provided document.

DIFFICULTY CONTRACT

Follow only the contract for the selected level:

{difficulty_instructions}

The cognitive task—not obscure wording—determines difficulty. Keep every question fully answerable from the CONTEXT.

CROSS-QUIZ UNIQUENESS

The following questions have already been used for this document. Do not repeat them, paraphrase them, test the same fact, or reuse the same correct-answer relationship:

{avoid_questions}

Select different concepts when possible. If the same broad topic must be used, test it through a genuinely different cognitive operation.

WITHIN-BATCH UNIQUENESS

The {num_questions} questions you generate in this single response must also be unique from one another:

* No two questions may test the same fact, concept, or correct-answer relationship, even if reworded.
* Do not center every question on the single most prominent concept in the CONTEXT. Spread the {num_questions} questions across different sections, concepts, or sub-topics of the CONTEXT.
* Each question must be solvable independently of the others.
* Before finalizing, compare every question against every other question in this response; rewrite or replace any pair that overlaps in meaning.

CONTENT REQUIREMENTS

* Use only information from the CONTEXT.
* Do not use outside knowledge or invent unsupported details.
* Every question must be fully answerable from the CONTEXT.
* Cover different important concepts and avoid duplicate or paraphrased questions.
* Use clear, natural, and unambiguous English.
* Avoid trick questions, negative wording, and double negatives.

ANSWER OPTION REQUIREMENTS

* Each question must have exactly 4 options: A, B, C, and D.
* Each question must have exactly one correct answer.
* The correct answer must be supported by the CONTEXT.
* Distractors must be plausible, relevant, and clearly incorrect.
* Options must be distinct, non-overlapping, and similar in length.
* Distribute correct answers across A, B, C, and D without an obvious pattern.

OUTPUT REQUIREMENTS

Return only one valid JSON object.

Do not use Markdown.
Do not wrap the JSON in a code block.
Do not include introductory text, comments, explanations, or any content outside the JSON.
Do not generate explanation, difficulty, source, page, chunk, topic, or metadata fields.
Escape every double quote inside question or option text as \".
Encode line breaks inside JSON strings as \n; never place a raw newline inside a string.
Finish and close every JSON string, array, and object before returning the response.

The output must follow this exact structure:

{
"questions": [
{
"question": "Question text in English",
"options": [
"A. Option A in English",
"B. Option B in English",
"C. Option C in English",
"D. Option D in English"
],
"correct_answer": "A"
}
]
}

FINAL SELF-CHECK

Before returning the JSON, verify that:

1. The output contains exactly {num_questions} questions.
2. Every question matches the selected difficulty level.
3. Each question has 4 distinct options and exactly one correct answer.
4. All questions are answerable from the CONTEXT.
5. There are no duplicate questions or overlapping options.
6. The correct answers are distributed without an obvious pattern.
7. The JSON is valid and contains no extra text.
8. No question repeats, paraphrases, or tests the same fact as a question in CROSS-QUIZ UNIQUENESS.
9. No two questions within this response repeat, paraphrase, or test the same fact, concept, or correct-answer relationship as each other.

CONTEXT:

{context}
